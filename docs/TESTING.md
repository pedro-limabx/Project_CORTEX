# Testing

Required for each module: unit tests; integration tests when external dependencies exist; regression tests; failure-path tests.

Sensitive actions additionally require authorization, replay/duplication and post-execution verification tests.

A phase is complete only when implementation, tests, documentation, security review and validation are all complete.
